# Sweet Bamboo Pattern Palette

Static tool for generating Weavy flatlay + lifestyle prompts across the Sweet Bamboo pattern range.

- index.html — the app (open in a browser)
- patterns/ — full-resolution pattern files used by the Download buttons

## Deploy (Vercel)
Static site, no build step. Framework preset: Other. No build command. Output = repo root.
